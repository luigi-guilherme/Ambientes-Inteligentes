@model List<UsuarioViewModel>
@{
    Layout = "~/Views/Shared/_Layout.cshtml";
    ViewData["Title"] = "Listagem de Usuários";
}

<h2>@ViewData["Title"]</h2>

<a href="/usuario/create" class="btn btn-primary mb-3">Novo Registro</a>

<table class="table table-striped table-hover">
    <thead>
        <tr>
            <th>Ações</th>
            <th>Id</th>
            <th>Nome</th>
            <th>Login</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var usuario in Model)
        {
            <tr>
                <td>
                    <a href="/usuario/edit?id=@usuario.Id" class="btn btn-sm btn-info">Editar</a>
                    <a href="javascript:confirmarExclusao(@usuario.Id)" class="btn btn-sm btn-danger">Apagar</a>
                </td>
                <td>@usuario.Id</td>
                <td>@usuario.Nome</td>
                <td>@usuario.Login</td>
            </tr>
        }
    </tbody>
</table>


<script>
    function confirmarExclusao(id) {
        Swal.fire({
            title: 'Tem certeza?',
            text: "Essa ação não poderá ser desfeita!",
            icon: 'warning',
            showCancelButton: true,
            confirmButtonColor: '#d33',
            cancelButtonColor: '#6c757d',
            confirmButtonText: 'Sim, apagar!',
            cancelButtonText: 'Cancelar'
        }).then((result) => {
            if (result.isConfirmed) {
                window.location.href = '/usuario/delete?id=' + id;
            }
        });
    }
</script>